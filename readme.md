--deployment important notes and cloning our repo which needs only necessary dependencies:
    pip freeze > requirements.txt -  take sonly necesary files when pushing code to github
    initialize git with - git init
    